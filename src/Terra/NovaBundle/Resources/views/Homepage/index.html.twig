{% extends '::base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
	<h1>Accueil</h1><br />
	{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
		<a href="{{ path('terra_nova_enseigant_index') }}">Enseignant</a><br />
		<a href="{{ path('fos_user_security_logout') }}">Déconnexion</a>
	{% else %}
		<a href="{{ path('terra_nova_enseigant_connection') }}">Go to enseignant</a>
	{% endif %}

{% endblock %}
