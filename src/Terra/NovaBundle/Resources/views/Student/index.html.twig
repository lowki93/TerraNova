{% extends '::base.html.twig' %}

{% block title %}Enseignant | Elévès de {% for className in class %}{{ className.name }}{% endfor %}{% endblock %}

{% block body %}
<h1>Classe Name : {% for className in class %}{{ className.name }}{% endfor %}</h1>

    <a href="{{ path('terra_nova_enseigant_index') }}">Back to enseignant</a>
    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Firstname</th>
                <th>Age</th>
                <th>Avatar</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('Eleve_show', { 'id': entity.id, 'idClasse': idClasse }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.firstName }}</td>
                <td>{{ entity.age }}</td>
                <td>{{ entity.avatar }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('Eleve_show', { 'id': entity.id, 'idClasse': idClasse }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('Eleve_edit', { 'id': entity.id, 'idClasse': idClasse }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
        <a href="{{ path('Eleve_new', { 'idClasse' : idClasse }) }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
