{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Student</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ entity.firstName }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ entity.age }}</td>
            </tr>
            <tr>
                <th>Avatar</th>
                <td>{{ entity.avatar }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
    <a href="{{ path('Eleve', {'id': idClasse}) }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('Eleve_edit', { 'id': entity.id, 'idClasse': idClasse }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
