{% extends '::baseAdmin.html.twig' %}

{% block title %} Admin | Trophée{% endblock %}

{% block body -%}
    <div id="breadcrumb">
        <a href="{{ path('trophy') }}" title="" class="tip-bottom">Trophés</a>
    </div>

    {% if entities %}
        <div class="container">
            <div class="row">
                {% for entity in entities %}
                    <div class="span3 reward-badge">
                        <div>Nom : {{ entity.name }}</div>
                        <img src="/img/trophy/{{ entity.path }}" alt="{{ entity.name }}" />
                        <div class="badge-hover">
{#                             <a href="{{ path('badge_show', { 'id': entity.id }) }}">voir</a><br /> #}
                            <a href="{{ path('trophy_edit', { 'id': entity.id }) }}">editer</a> 
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    {% else %}
        <p>Il n'y a pas de trophé</p>
    {% endif %}
    <a href="{{ path('trophy_new') }}">Créer un trophé</a>

{% endblock %}
